{
  "name": "derail",
  "version": "0.1.0",
  "description": "Record every Soroban deploy attempt — including the ones that never produced a contract.",
  "type": "module",
  "bin": {
    "derail": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
