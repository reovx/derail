{
  "name": "derail",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web && npm run build --workspace derail",
    "lint": "npm run lint --workspace web",
    "typecheck": "npm run typecheck --workspace web && npm run typecheck --workspace derail"
  }
}
